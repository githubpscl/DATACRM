import { Router } from 'express';

const router = Router();

// Placeholder for simple AI routes
router.get('/', (req, res) => {
  res.json({ message: 'Simple AI endpoint' });
});

export default router;
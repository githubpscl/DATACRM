import { Router } from 'express';

const router = Router();

// Placeholder for data import routes
router.get('/', (req, res) => {
  res.json({ message: 'Data import endpoint' });
});

export default router;